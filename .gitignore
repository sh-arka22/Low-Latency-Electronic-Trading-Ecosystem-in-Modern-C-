# Build output
cmake-build-release/
cmake-build-debug/
build/
out/
*.o
*.a
*.so
*.dylib
*.exe

# CMake generated
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json
CTestTestfile.cmake
_deps/

# Log files generated at runtime
*.log
stderr.txt

# Latency histograms dumped by TradeEngine at shutdown (Day 1, regeneratable
# via scripts/run_demo.sh). The rendered plot in docs/latency.png is tracked;
# the raw .hgrm data is not.
latency_*.hgrm

# macOS metadata
.DS_Store
._*

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~
.cache/
.ccls-cache/
.clangd/
*.sublime-project
*.sublime-workspace

# Coredumps
core
core.*
vgcore.*

# Profiling
*.gcda
*.gcno
*.gcov
perf.data
perf.data.old

# Generated notebook outputs
notebooks/*.html

# Generated backtest data — Binance tape downloads, merged .tape files,
# and the per-strategy PnL CSVs produced by scripts/run_full_sweep.sh.
# Repro: bash scripts/run_full_sweep.sh (takes ~6 hours).
data/

# Local Claude Code agent settings + worktrees (per-user, not shareable)
.claude/
